spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

  management:
    endpoints:
      web:
        exposure:
          include: info, health, metrics